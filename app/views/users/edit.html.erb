<% if logged_in? && !current_user.role?(:admin) %>
	<% header = "Reset Password" %>
<% else %>
	<% header = "Edit User" %>
<% end %>

<%= render partial: "partials/display_form", locals: {
	header: header} %>