<h2>Customer details</h2>

<table>
  <tr> 
    <td width="350">
      <%= render partial: "customer_data" %>
    </td>
    <td width="30">&nbsp;</td>
    <td width="220">
      <% unless @order_history.empty? %>
			  <%= render partial: "order_history" %>
			<% end %>
    </td>
  </tr>
</table>

<p class="smtext">&nbsp;</p>
<p>&nbsp;
  <%= link_to "Edit", edit_customer_path(@customer) %> |
  <%= link_to "Destroy", @customer_destroy, method: :delete, data: { confirm: 'Are you sure you wish to delete this information?' } %> |
  <%= link_to "View Customers", customers_path %>
</p>

<p id="notice"><%= notice %></p>