<div class="row">
  <% items.each do |item| %>
  <div class="col s4">
    <div class="card small">
      <div class="card-image">
        <% if item.picture_url.nil? %>
          <%= image_tag("no_image_large.png") %>
        <% else %>
          <%= image_tag(item.picture_url, height: "200") %>
        <% end %>
      </div>
      <div class="card-content image">
        <a href=<%=item_path(item) %>><span class="card-title"><%= item.name %></span></a>
        <p><%= number_to_currency item.current_price %></p>
      </div>
    </div>
  </div>
  <% end %>
</div>