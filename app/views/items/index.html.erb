<div class="flat-card">
	<div class="card-content">
		<h2>Items</h2>

		<%= render partial: "item_bread" %>
		<%= render partial: "item_muffins" %>
		<%= render partial: "item_pastries" %>
	</div>
</div>