<h6><i class="mdi-navigation-chevron-left"></i><a href="/items">Items</a></h6>

<% if @item.picture_url.nil? %>
	<%= image_tag("no_image_large.png", align: "center") %>
<% else %>
	<br>
	<img class="materialboxed" width="280" height="202" src=<%= @item.picture_url %>>
<% end %>